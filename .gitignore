# local deploy/host config — not source
.env

# local Claude Code settings
.claude/

# dev tooling — the deploy artifact is the single html file
node_modules/

# private provenance (author's personal notes)
PROVENANCE-PRIVATE.md
